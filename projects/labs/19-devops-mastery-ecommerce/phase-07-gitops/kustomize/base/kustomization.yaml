apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources generated from Helm template output.
# To regenerate:
#   helm template ecommerce-service ../../../phase-06-kubernetes/helm \
#     -f ../../../phase-06-kubernetes/helm/values/production.yaml \
#     --output-dir ./generated
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: ecommerce
